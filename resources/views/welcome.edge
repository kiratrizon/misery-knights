<!DOCTYPE html>
<html lang="en" class="scroll-pt-16 scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- seo -->
  <meta name="description" content="Join {{ env('APP_NAME') }} - a guild united by honor, forged in battle, conquering Althea together." />
  <meta name="keywords" content="gaming, guild, PvE, PvP, community" />
  <link rel="icon" type="image/x-icon" href="/images/emblem.png" />
  <title>{{ env('APP_NAME') }}</title>
  <!-- Tailwind CDN -->
  @vite(['resources/ts/app.ts'])
</head>
<body class="bg-gray-900 text-gray-100">
  <!-- Navbar -->
  <nav class="fixed top-0 left-0 w-full z-50 bg-gray-900/80 backdrop-blur border-b border-gray-800">
    <div class="max-w-7xl mx-auto px-6">
      <div class="flex items-center justify-between h-16">

        <!-- Logo / Name -->
        <a href="#" class="text-xl font-extrabold text-orange-400 tracking-wide">
          {{ env('APP_NAME') }}
        </a>

        <!-- Desktop Menu -->
        <div class="hidden md:flex items-center gap-8">
          <a href="#about" class="text-gray-300 hover:text-orange-400 transition">About</a>
          <a href="#gallery" class="text-gray-300 hover:text-orange-400 transition">Gallery</a>
          <a href="#join" class="px-5 py-2 rounded-lg bg-orange-500 text-gray-900 font-semibold hover:bg-orange-400 transition">
            Join
          </a>
        </div>

        <!-- Mobile Button -->
        <button id="menuBtn" class="md:hidden text-gray-300 focus:outline-none">
          ☰
        </button>

      </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="hidden md:hidden bg-gray-900 border-t border-gray-800">
      <div class="px-6 py-4 flex flex-col gap-4">
        <a href="#about" class="text-gray-300 hover:text-orange-400">About</a>
        <a href="#gallery" class="text-gray-300 hover:text-orange-400">Gallery</a>
        <a href="#join" class="text-orange-400 font-semibold">Join</a>
      </div>
    </div>
  </nav>
  <!-- Header / Hero -->
  <header class="relative bg-cover bg-center h-[70vh]" style="background-image: url('/images/banner.png');">
    <!-- Overlay -->
    <div class="absolute inset-0 bg-black/60"></div>

    <div class="relative z-10 flex flex-col items-center justify-center h-full text-center px-4">
      <h1 class="text-4xl md:text-6xl font-extrabold tracking-wide text-orange-400">{{ env('APP_NAME') }}</h1>
      <p class="mt-4 max-w-2xl text-lg md:text-xl text-gray-200">
        United by honor, forged in battle, conquering Althea together.
      </p>
      <div class="mt-6 flex gap-4">
        <a href="#about" class="px-6 py-3 rounded-xl bg-orange-500 text-gray-900 font-semibold hover:bg-orange-400 transition">About Us</a>
        <a href="#join" class="px-6 py-3 rounded-xl border border-orange-500 text-orange-400 hover:bg-orange-500 hover:text-gray-900 transition">Join Guild</a>
      </div>
    </div>
  </header>

  <!-- About Section -->
  <section id="about" class="py-16 px-6 max-w-6xl mx-auto">
    <h2 class="text-3xl font-bold text-center text-orange-400">About the Guild</h2>
    <p class="mt-6 text-center text-gray-300 max-w-3xl mx-auto">
      We are a {{ env('APP_NAME') }} focused on PvE, PvP, and community growth. Whether you're a casual adventurer
      or a hardcore raider, our guild welcomes you.
    </p>

    <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="bg-gray-800 rounded-2xl p-6 shadow-lg">
        <h3 class="text-xl font-semibold text-orange-400">PvE Raids</h3>
        <p class="mt-3 text-gray-300">Weekly nests, dragons, and end-game content with organized teams.</p>
      </div>
      <div class="bg-gray-800 rounded-2xl p-6 shadow-lg">
        <h3 class="text-xl font-semibold text-orange-400">PvP Battles</h3>
        <p class="mt-3 text-gray-300">Guild wars, ladder matches, and friendly duels to sharpen your skills.</p>
      </div>
      <div class="bg-gray-800 rounded-2xl p-6 shadow-lg">
        <h3 class="text-xl font-semibold text-orange-400">Community</h3>
        <p class="mt-3 text-gray-300">Active Discord, helpful members, and a fun, respectful environment.</p>
      </div>
    </div>
  </section>

  <!-- Gallery Section -->
  <section id="gallery" class="py-16 bg-gray-800">
    <div class="max-w-6xl mx-auto px-6">
      <h2 class="text-3xl font-bold text-center text-orange-400">Guild Gallery</h2>
      <p class="mt-4 text-center text-gray-300">Here are some screenshots and artworks from our guild members:</p>

      <div class="mt-10 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        <!-- Sample image sizes -->
        <div class="rounded-xl overflow-hidden">
          <!-- 600x400 recommended for thumbnails -->
          <img src="/images/1.png" alt="Guild Screenshot" class="w-full h-full object-cover" />
        </div>
        <div class="rounded-xl overflow-hidden">
          <!-- 600x400 recommended for thumbnails -->
          <img src="/images/2.png" alt="Raid Screenshot" class="w-full h-full object-cover" />
        </div>
        <div class="rounded-xl overflow-hidden">
          <!-- 600x400 recommended for thumbnails -->
          <img src="/images/banner.png" alt="PvP Screenshot" class="w-full h-full object-cover" />
        </div>
      </div>

      <p class="mt-6 text-center text-sm text-gray-400">
        <!-- some description -->
         All images are property of their respective guild members.
      </p>
    </div>
  </section>

  <!-- Join Section -->
  <section id="join" class="py-16 px-6 max-w-4xl mx-auto text-center">
    <h2 class="text-3xl font-bold text-orange-400">Join Our Guild</h2>
    <p class="mt-4 text-gray-300">
      Interested in joining? Contact our guild leaders in-game or join our Discord server.
    </p>

    <div class="mt-8 flex flex-col sm:flex-row justify-center gap-4">
      <button class="px-8 py-3 rounded-xl bg-orange-500 text-gray-900 font-semibold hover:bg-orange-400 transition hover:cursor-pointer" onclick="openModal()">Apply In-Game</button>
      <a href="https://discord.gg/QBGXFcfm5E" target="_blank" class="px-8 py-3 rounded-xl border border-orange-500 text-orange-400 hover:bg-orange-500 hover:text-gray-900 transition">Join Discord</a>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-950 py-6 text-center text-gray-500 text-sm">
    © 2026 {{ env('APP_NAME') }}. All rights reserved.
  </footer>



  <!-- modals -->
  @component('layouts/modals')
  @endcomponent
  <script>
    const btn = document.getElementById('menuBtn');
    const menu = document.getElementById('mobileMenu');

    btn.addEventListener('click', () => {
      menu.classList.toggle('hidden');
    });


    function openModal() {
      document.getElementById("modal").classList.remove("hidden");
    }

    function closeModal() {
      document.getElementById("modal").classList.add("hidden");
    }
  </script>
</body>
</html>
